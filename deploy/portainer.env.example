# ============================================================
# Consultio Med - Variaveis de Ambiente
# ============================================================
# Cole este bloco inteiro no campo "Environment variables"
# ao criar a stack no Portainer.
#
# Para Docker Desktop:
#   docker compose -f deploy/portainer-stack.yml --env-file deploy/portainer.env.example up -d
# ============================================================
#            !!!  ALTERE AS SENHAS EM PRODUCAO  !!!
# ============================================================

# --- SEGURANCA (GERAR VALORES UNICOS!) ---
# Gere com: openssl rand -base64 32
JWT_SECRET=hg5zmYzA2tlcc1lz2bWha8e7/pxFTi73ILo8IVToIdY=
# Gere com: openssl rand -hex 32
ENCRYPTION_MASTER_KEY=271d251b3cfb545ca45362e9d476c7dd5c5561e1c23d7571b74b5c336ceff138

# --- BANCO DE DADOS (PostgreSQL interno - recomendo) ---
DB_PASSWORD=5af64116c019cec8f026c7f9

# --- WHATSAPP ---
WHATSMEOW_WEBHOOK_SECRET=XJ2mHxDdEhX3DtgnGygT5A==
WHATSMEOW_API_TOKEN=

# --- GEMINI AI (OPCIONAL) ---
# Obtenha em: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- APLICACAO ---
# Para producao com dominio: APP_URL=https://app.seudominio.com.br
APP_URL=http://localhost:5173
# Em producao, troque para o dominio real: CORS_ORIGIN=https://app.seudominio.com.br
CORS_ORIGIN=http://localhost:5173

# --- DOMINIO E HTTPS (Caddy - OPCIONAL) ---
# So preencha se tiver dominio configurado apontando para o servidor
APP_DOMAIN=http://localhost:5173
ACME_EMAIL=admin@consultio.local

# --- PLATAFORMA ---
PLATFORM_OWNER_EMAIL=owner@consultio.local

# --- SUPABASE (OPCIONAL - so use se migrar do Supabase) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
